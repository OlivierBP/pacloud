AWSTemplateFormatVersion: 2010-09-09
Parameters:
  WebInstanceType:
      Description: >-
            EC2 instance type for the app tier. Must be a free instance when on my own
	          account !
		      Type: String
		          Default: t2.micro
			      AllowedValues:
			            - t2.micro
				          - t2.medium
					      ConstraintDescription: Must be a valid instance type.
					        AppInstanceType:
						    Description: >-
						          EC2 instance type for the app tier. Must be a free instance when on my own
							        account !
								    Type: String
								        Default: t2.micro
									    AllowedValues:
									          - t2.micro
										        - m4.xlarge
											    ConstraintDescription: Must be a valid instance type.


















